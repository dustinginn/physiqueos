# Midweek V3 pre-deploy integrity corrections — FINAL reviewed candidates

Generated: 2026-09-25
Task id: `claude-midweek-v3-predeploy-integrity-final-20260925`
Agent: Claude (Midweek Briefing Founder Takeover lane, secondary)
Supersedes: `20260925T083000Z-midweek-v3-release-blockers-final.md` (Server `092011cc`). The timezone precheck `20260925T140000Z-midweek-v3-timezone-authority-readonly-precheck.md` stands as the read-only predeploy checkpoint.
Status: **CODE / TEST / REVIEW COMPLETE. STOPPED FOR FOUNDER DEPLOYMENT AUTHORIZATION.**

**Nothing was deployed. Nothing was uploaded (no archive, no TestFlight). No production data, policy, or workout was mutated. No historical briefing was regenerated. Cardio is NOT activated. No build-number change.** The only production contact in this task's lineage was the earlier authorized read-only precheck (`BEGIN … READ ONLY`, rolled back). This task's fixes made no production access. `latest.json`/`latest.md` untouched.

## Exact final candidates

| | SHA | Branch (pushed, not merged) |
|---|---|---|
| **Server** | `e88b8ef78fa236ce09660997f4084bde069018a7` | `claude/midweek-v3-engine-server-20260925` |
| **Native** | `2374e11aa707ba4124378958ace429ffd781feba` | `claude/midweek-standard-format-v3` — **unchanged** |

Server delta vs the previously reviewed `092011cc`: `714191fc` (Fix B), `172b525b` (Fix A + N3 tests), `e88b8ef7` (timezone tests) — 22 files, +1562/−86. No migration/schema/`.sql`/package file anywhere in `01d1900b..e88b8ef7`. No Server contract change reached Native, so **no Native work, no Native run** (the earlier clean full run on `2374e11a` and its PASS-WITH-NOTES review carry forward). Production is still on `01d1900b` (deployment `8da160ac` ACTIVE, reverified at task start; web+worker `01d1900b`).

## FIX A — coherent settlement snapshot (review N2, was a release blocker)

**Bug:** the cadence tick read the HealthKit evidence overlay once at tick start, but the gate reset the reader (`beginTick -> beginRun`) and re-read coverage later; a domain settling in between could produce a watermark claiming revision N while the generator froze N-1.
**Solution (preferred pattern 1 — derive readiness from the same snapshot that is frozen):** `HealthKitGraduationReader` keeps, per run, ONE memoized read of the canonical HealthKit day list and ONE policy lookup; the evidence overlay and `readSettlementCoverage` both use it (reads per tick: 2 -> 1). The composition owns the run (`beginRun` first, `endRun` in `finally`, including when the tick throws); the gate's `beginTick` now adopts that run (`beginSettlementRun`) instead of resetting it; a stand-alone gate still gets one snapshot per tick. Cached read rejections surface to BOTH overlay (falls back, sets overlay-failure) and coverage (`readError`, fail-closed) and are cleared by the next run so the next tick recovers. With no run begun, reads are fresh (long-lived-worker semantics preserved). `describeSnapshot()` returns a PII-free sha256 digest of (domain, localDate, canonicalRecordId, revision, coverage) tuples (used by tests; not persisted).
**Invariant now holds:** readiness decision, watermark record/revision identities and generator inputs all derive from the tick-start snapshot, even for a later cadence in the same tick after a slow earlier generation. Evidence advancing after the snapshot is picked up by the NEXT tick and never mutates a frozen artifact. Hard deadline, fail-closed read errors, and no-duplicate behaviour intact. No refresh loops needed.
**Tests (11 new, real executor + gate + reader + Midweek generator):** overlay at N-1 with the store advancing to N before the gate -> watermark, artifact and generator inputs all N-1 (never N-with-N-1); partial day settling after the snapshot -> waits, next tick generates at N coherently; evidence advancing mid-generation -> frozen artifact/watermark unchanged; read-error then recovery; rejected snapshot shared by both consumers; hard deadline on one coherent partial snapshot with honest unsettled state; two-worker modeled race -> one coherent artifact/watermark; later cadence in the same tick uses the same snapshot (1 list read total); no-run = fresh reads; run boundary/adoption. Composition +2 tests (one day-list query per tick while the store advances; `endRun` even when the tick throws). Mutations (all RED, restored): coverage does a fresh list in-run 9 RED; `beginTick` resets the run 7 RED; separate overlay/coverage snapshots 9 RED. Orchestrator spot-check on final HEAD: `beginTick`-reset mutation 7 RED, restored 209/209 green.

## FIX B — one canonical recurring-briefing timezone authority

**Production facts (read-only precheck):** stored `coachingUpdates.timeZone` = `America/Los_Angeles`; `user.timeZone` absent; the scheduler resolved coaching-first while Midweek/Weekly/Monthly generators used `user?.timeZone ?? "America/Los_Angeles"` — equal only because the stored value equals the hard-coded default (a future-user trap).
**Solution:** `resolveBriefingTimeZoneAuthority({coachingUpdates, user})` in `BriefingScheduleAuthority` (explicit coaching timezone -> `user.timeZone` -> legacy `user.timezone` -> product default LAST; blanks ignored) and a new `RecurringBriefingTimeZoneAuthority.resolveRecurringBriefingTimeZone({repositories,userId,user,suppliedTimeZone})` (stored state only — no parameter for any device/travel/request zone). The registry stamps each entry with the resolved zone + source; the executor passes `entry.timeZone` to the generators; Midweek, Weekly (incl. catch-up/`buildWeeklyArtifact`) and Monthly (eligibility + regeneration fallback) use it and otherwise resolve through the SAME helper, so direct/manual invocations agree with the scheduler. Explicit `windowOverride` keeps its own window's zone. Extra sites aligned (Founder-neutral, reviewed low-risk): `HomeBriefingService` recurring expectation, `BriefingReconciliationEnqueueService` publication pick, Midweek/Weekly-V4 preview services; `CoachingUpdatesReadService` fallback now also honours legacy `user.timezone`.
**Watermark:** `timeZone` = the exact authority-resolved zone; `timeZoneAuthority` = `briefing_schedule_authority:<coaching_updates|user_profile|product_default>` (was a constant; no consumer compares the old string). `attachEvidenceSettlement` guard TIGHTENED: exact window id AND `watermark.timeZone == evidenceWindow.timeZone` (replaces the review-N1 timezone-only tolerance, which existed only because of the split); `createSettlementGeneratorInput` also refuses a cadence whose scheduled zone differs from its window zone. Only the executor supplies `settlement`, so no override/regeneration/catch-up/existing-artifact path can be blocked by the stricter guard (reviewer-verified).
**Not changed by design:** DEXA/Photo/event briefings and legacy daily briefings (source-scan test proves the event services never use the recurring authority). Monthly stays day 1.
**Tests (30 new):** precedence + read-model source; hourly sweeps with the real registry, real coaching-updates read service and real generators comparing window id/start/end/zone for scheduler vs generator-with-`entry.timeZone` vs generator-with-no-zone — coaching Tokyo/Auckland vs user LA, coaching absent + user zone, protocol without explicit zone, both legacy user spellings, both absent; LA fall-back 2026-11-01, LA spring-forward 2027-03-14, Sydney DST start 2026-10-04; a case where the zone difference moves the local day; Monthly day-1 over 62 hourly-swept days (only 2026-10-01 / 2026-11-01); **Founder production-shaped fixture** (coaching LA explicit, `user.timezone` null) -> exactly today's windows (`midweek:2026-09-13:2026-09-15:America/Los_Angeles`, weekly 09-20..09-26, monthly 09-01..09-30); watermark zone == generator window zone for all three cadences via the real executor; travel/device-input immunity + an 8-file source scan; no `user?.timeZone ??` chains left in the recurring files. Mutations (all RED, restored): Midweek reverted 9 RED, Weekly 9, Monthly 8; zone guard disabled 2; executor stops passing `timeZone` 7 (orchestrator spot-check on final HEAD: 7 RED, restored 209/209); executor + Midweek fallback ignoring coaching 15.
**Existing tests edited (intended contract changes, none weakened):** watermark N1 test (tolerance -> stricter refusal + positive control), `timeZoneAuthority` expectation string, `BriefingSchedule3AM` generator call keys (+`timeZone`, value asserted LA), composition executor-stub additive hook.

## N3 — ACCEPTED fail-safe (unchanged behaviour)
An unexpected non-coverage gate exception still fails closed with NO hard-deadline escape by Founder decision: unknown programming/config errors must not force publication of a possibly-invalid strategic artifact. Verified by 6 new tests: logged as a `briefing_settlement.settlement_gate_error` warn with error class name + code only (no message/stack; unchanged by the real `redactStructuredValue`, tested with an email/token/host in the message); entry returns `awaiting_evidence_settlement`, retryable; never generates (including past the deadline); six repeated ticks write no artifact and no `generation_started` record, exactly one execution record per tick; after the error is fixed the next tick creates exactly one artifact; other cadences in the same tick still run. Documented in the gate file's header. **Future ops work:** alert on a sustained `settlement_gate_error` rate.

## Concurrency
Real Postgres two-worker verification remains **DEFERRED** by decision (no local Postgres harness in the repo: no pg-mem/PGlite/embedded-postgres/testcontainers/docker/postgres binary; `test:*:postgres` scripts require an externally provisioned URL, not used). Deterministic executor/gate/reader/generator/repository/publication two-worker tests remain green (`BriefingCadenceConcurrency` 9, `BriefingPublicationConcurrency` 3, plus the new snapshot race). The advisory-lock code path (`PostgresBriefingCadenceExecution*`) is unchanged since `092011cc`. Unproven: advisory-lock hashing, row serialization, `baseline_conflict` retry, true cross-process interleaving. No production database was used for concurrency.

## Validation
- **Webpack:** `NEXT_PHASE=phase-production-build npm run build -- --webpack` on the EXACT final SHA `e88b8ef7`: exit 0, "Compiled successfully", no "Failed to compile", tree clean afterwards.
- **Part F: 71/71** (47 + 24) — green in the final reviewer run and in the 209/209 orchestrator suite (settlement, executor, concurrency, snapshot, timezone, watermark, composition, schedule, Part F).
- **Broad** (`src/domain src/application src/platform/database src/data`): 200 failing tests at `e88b8ef7`, 200 on clean production `01d1900b` — the SETS ARE IDENTICAL (no new failures, none fixed); all environmental/pre-existing (gitignored `private/founder/*` data absent from the worktree; the two `HealthKitStrategicReadBoundary` offenders `HealthKitWorkoutPresentationService.js`/`HealthKitDeferredWorkoutReconciliationRunner.js`; `HomeBriefingAvailability` source scan; `PostgresFounderRepositoryFacade` nested-weight `RangeError`; etc.).
- **Final fresh-context review (`e88b8ef7`): PASS-WITH-NOTES, no blocking.** Fix A: one coherent snapshot verified by trace (no remaining second read; cached-rejection recovery; `endRun` in `finally`; reads per tick 2 -> 1; N3 intact). Fix B: every recurring timezone site traced; precedence correct; device/travel timezone cannot reach these paths; Founder shape unchanged; Monthly day 1 and event-briefing isolation meaningful (not vacuous); stricter watermark guard cannot block a legitimate path. HealthKit `c58dcca9` files (`nativeCommandRequestBounds`, `HealthKitObservationService`, `HealthKitWorkoutService`, Cardio fixture, tests, platform/operations) byte-identical; Cardio readiness tooling preserved; no policy activation, no data mutation, no migration.
- **Native:** `2374e11a` unchanged; full run and review carried forward; no Native run.

## Review notes — disposition (all non-blocking)
- **N-A (low) ACCEPTED:** reader run state is single-tenant; two overlapping `execute()` calls on one runner could break each other's run (re-introducing the N2 race). Not reachable: the worker loop is strictly serial (`BriefingCadenceWorker` awaits each tick) and there is one reader per runner. Follow-up: add a run-token/reentrancy guard.
- **N-B (low, future users) ACCEPTED, pre-existing:** `WeeklyClosedWindowContract` hard-restricts Weekly closed-window catch-up to `America/Los_Angeles`; a non-LA user's catch-up would be rejected (fail-closed) while the scheduler runs in their zone. Founder unaffected. Follow-up before onboarding non-LA users.
- **N-C (low) ACCEPTED:** non-recurring `user?.timeZone ?? LA` chains remain in daily/presentation/event scope (`HomeBriefingService` daily window, `DailyBriefingService`, `BriefingNavigationReadService`, `PhaseAwareActiveGoalPreviewService`, a read-model fallback in `CanonicalPersistenceCommandPorts`) — none builds a recurring strategic window.
- **N-D (info):** the composition test uses a mocked executor; the snapshot tests replicate the composition sequence through a helper, so real composition + real executor is not exercised end-to-end in one test.
- **N-E/N-F (info):** Progress Hub/Evidence read stores also `beginRun` per request and now share one day-list read per run (fewer reads, no cross-request staleness; stores are per-request); `resolveBriefingTimeZone` now treats blank strings as absent.
- **Carried from the prior report, still accepted:** N4 (concurrency infra above), N5 (Weekly/Monthly generators return `completed` without an `idempotent` flag; a matched result over a watermark-less artifact could be logged as created), N6 (`effectiveAt` cut by UTC date, <= 1 day mixed regime), N7 (new contract/interpretation ids after deploy), N8 (decision-time vs commit-time events), N9 (one extra full-history stringify per generation), N10 (returned artifact can differ from the stored older watermark in authorized replacement).
- **Watermark digest not persisted:** `describeSnapshot().digest` could be added to the watermark (would touch the watermark builder/integrity digest); intentionally deferred — coherence is proven via read counts and revision equality.

## Disk-space compliance (STANDING_DISK_SAFETY)
Every heavy operation ran with >= 16 GiB free (17 GiB before/after each; floor 15 GiB, preferred 20 GiB only for Native — none run). No Native build/archive/suite. Temporary comparison worktrees created for baseline failure diffs were removed. Free space before/after this task: 17 GiB -> 17 GiB.

## Recommended release sequence (NOT executed — needs Founder authorization)
1. Founder reviews this report; accepts N-A/N-B/N-C and the carried notes.
2. Merge `claude/midweek-v3-engine-server-20260925` @ `e88b8ef7`; deploy the Server via the two-step (`apps update --spec` bumping `PHYSIQUEOS_GIT_SHA`/`PHYSIQUEOS_BUILD_ID` on BOTH web and worker, AND `create-deployment --force-rebuild`); verify `source_commit_hash` on both components and the log gitSha; health `/api/v1/health/live` + `/ready`. No migration. Server-first is safe (Native `2374e11a` emits `isIndoorWorkout` only when known).
3. Watch the first cycles: `briefing_settlement.*` events, `evidenceSettlement` on the next Midweek/Weekly artifact (expected timezone `America/Los_Angeles`, authority `briefing_schedule_authority:coaching_updates`), `settlement_gate_error` absent, Energy variability conservative.
4. Separate authorization: merge Native `2374e11a`; Build 60 bump/archive; separate authorization for TestFlight upload.
5. Founder device acceptance of Strength (Sep23/Sep24) and Midweek; then the prepared Cardio graduation gates (Cardio remains NOT activated).
6. Later: real-Postgres two-worker run against a disposable database; run-token guard (N-A); non-LA onboarding items (N-B).

## Flags
AUTHORITY_REVERIFIED · TIMEZONE_PRECHECK_PUBLISHED · SETTLEMENT_COHERENT_SNAPSHOT_PASS · WATERMARK_INPUT_REVISION_CONSISTENCY_PASS · CANONICAL_BRIEFING_TIMEZONE_UNIFIED · FOUNDER_LA_TIMEZONE_BEHAVIOR_PRESERVED · TRAVEL_TIMEZONE_DOES_NOT_CHANGE_STRATEGIC_WINDOW · MONTHLY_DAY1_UNCHANGED · N3_FAIL_CLOSED_ACCEPTED · DETERMINISTIC_CONCURRENCY_PASS · REAL_POSTGRES_CONCURRENCY_DEFERRED · PART_F_71_TESTS_PASS · MUTATION_GUARDS_PASS · PRODUCTION_WEBPACK_BUILD_PASS · HEALTHKIT_TYPE_FIDELITY_PRESERVED · CARDIO_READINESS_TOOLING_PRESERVED · NATIVE_2374E11A_UNCHANGED_OR_REVALIDATED · FRESH_CONTEXT_SERVER_REVIEWED · FINAL_GH_REPORT_PUBLISHED
FALSE (as required): SERVER_DEPLOYED · TESTFLIGHT_UPLOADED · PRODUCTION_MUTATED
