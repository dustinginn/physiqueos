# Combined App Platform and persistence cutover

Status: implementation and isolated rehearsal design only. This document is not a production migration authorization. Windows, legacy JSON, and local production media remain authoritative until a new, exact combined-cutover authorization is issued.

## Why the transition is combined

A persistence-only transition cannot complete while the accepted PostgreSQL firewall permits only App Platform and the Windows serving runtime cannot consume the new canonical store. Moving the full application first also cannot complete because App Platform's ephemeral filesystem cannot provide the shared, durable semantics required by the legacy JSON and media implementation. A bridge or a temporary durable legacy store would create another authority plane and an immediate second migration.

The selected transition therefore moves serving, commands, reads, domain persistence, media, migration control, worker, and outbox authority together:

`Windows Next.js + legacy JSON/media -> fenced combined cutover -> App Platform Next.js/worker + PostgreSQL/Spaces`

The web product remains a first-class client. A future iOS client will call the same versioned application/API boundary. Neither client owns canonical storage; both observe PostgreSQL and private Spaces through the shared server application layer.

## Authority map

| Concern | Before the cutover | After accepted cutover |
| --- | --- | --- |
| Web serving and public routing | Windows production task and its current route | App Platform full Next.js web service and provider route |
| Canonical commands and reads | Windows application over Founder JSON | App Platform application boundary over PostgreSQL |
| Founder runtime | Versioned legacy JSON on Windows | PostgreSQL canonical tables and runtime metadata |
| Production media | Windows local evidence tree | Private, versioned Spaces objects plus PostgreSQL ownership records |
| Migration/runtime control | Protected Windows control and runtime-authority record | Transactional PostgreSQL control and runtime-authority record |
| Worker and outbox | Existing Windows-owned behavior; provider worker inert for production | One authority-gated App Platform worker and PostgreSQL outbox |
| Operational control | Windows task and protected local operations | App Platform health/readiness/operations with strong server-side authorization |
| Recovery | Current Windows build, JSON/media, and encrypted packet | Provider-compatible code rollback, PostgreSQL recovery/forward repair, Spaces versions; retained Windows data is recovery evidence only |

No intermediate phase has two canonical writers. PostgreSQL and Spaces may contain validated noncanonical copies before authority transfer, but those copies do not become canonical merely by existing.

## Machine-checkable invariants

The durable runtime-authority tuple binds environment, operation ID, authorization fingerprint, fence ID, final snapshot digest, Founder revision/digest, media manifest digest, migration-control digest, Windows source/build, provider source/build/deployment, PostgreSQL target, Spaces target, and routing target.

- `windows-legacy-authoritative` is the only state in which Windows may accept canonical writes.
- `combined-cutover-in-progress` requires an active matching Windows fence. No application accepts canonical writes.
- `provider-prepared` requires exact transfer receipts, PostgreSQL/Spaces semantic parity, provider readiness, and matching control/worker acknowledgements. It is non-public and write-blocked except for explicitly synthetic acceptance.
- `provider-authoritative` is entered once for the exact tuple. Windows can never regain write authority from this state.
- `recovery-required` preserves provider/PostgreSQL authority while writes may be paused for compatible-code rollback and forward repair.
- A provider canonical command must atomically verify `provider-authoritative` and record the first-provider-write marker in the same PostgreSQL transaction as its canonical mutation.
- A Windows command must fail closed whenever its protected control is fenced or no longer Windows-authoritative.
- A provider upload is noncanonical until private object readback validation and the final PostgreSQL ownership transaction succeed. Failure aborts multipart upload or deletes the exact uploaded version.
- The worker polls only when the same provider-authoritative tuple is complete; leases, retries, dedupe keys, and terminal receipts remain PostgreSQL-durable.
- No runtime falls back to Founder JSON, local media, or ephemeral container disk in provider canonical mode.

## Production phase model

1. **A — pre-fence validation.** Re-read live source/build, Founder revision and SHA-256, media inventory, control digest, targets, firewall, backups, provider compatibility deployment, routing preparation, capacity, alerts, and exact new authorization. Abort without mutation on drift.
2. **B — Windows fence.** Activate the protected Windows write fence for the exact operation. Reads follow the maintenance contract; all canonical commands and upload completion are rejected. Drain in-flight work and prove the fence across Workout Logger, evidence, weight/check-in, priorities, goals/protocols, and uploads.
3. **C/D — final capture/package.** After the drain, capture one authoritative JSON/media/control snapshot and create one deterministic package-v2 identity. Re-read the source after capture to prove it did not change behind the fence.
4. **E — one-time transfer.** Transfer only the authorized package and its declared media manifest over TLS to the provider execution boundary. Chunks are scoped to operation, artifact, ordinal, byte range, size, and SHA-256. Replays are idempotent only when bytes and metadata match; drift is rejected. The final receipt binds the complete manifest and authorization fingerprint. This is a cutover operation, not a continuing synchronization service.
5. **F/G — non-public import.** Import domain state transactionally into the exact PostgreSQL target and copy media privately into versioned Spaces. Objects remain noncanonical until manifests, ownership, MIME, length, hash, and version IDs validate.
6. **H/I/J — provider validation.** Validate complete semantic read parity, command readiness without legacy writes, media reads/uploads, full product route inventory, health, readiness, worker/outbox, and source/build identity while provider public writes remain disabled.
7. **K — durable preparation.** Record exact transfer receipts and provider/control/worker readiness. Both sides must show the same tuple. Windows records the provider acknowledgement while retaining its fence.
8. **L — authority/routing handoff.** Atomically transition provider runtime authority, keep Windows fenced, enable the prepared App Platform route, and verify that only the provider can reach the canonical command boundary. Routing must not lead this step.
9. **M — combined first-write boundary.** The first real canonical production command accepted through the provider application is committed with the first-write marker. This is the irreversible boundary.
10. **N/O — release and smoke.** Release writes only through the provider platform, start the authority-gated worker, and perform immediate web/read/write/media/user-facing acceptance. Windows remains stopped or read-only and cannot accept writes.
11. **P — stabilization.** Enter the approved observation period, reconcile domain/media/outbox state, verify backups and cross-client semantics, and retain legacy recovery material for at least the approved 35 days and until every exit condition passes.

## Fence and final snapshot ownership

Windows owns the fence through final snapshot capture and provider-prepared acknowledgement. The fence denies every canonical command, including logger finish, evidence confirmation, weight/check-in, priority completion, goal/protocol edits, and upload completion. Reads may continue from the frozen legacy snapshot with an explicit maintenance status. Provider control does not become authoritative at import time; it becomes authoritative only during the single authority transition after exact preparation evidence exists.

## One-time authenticated transfer

The transfer initiator supplies an operations-authenticated request whose digest is bound into the separately approved authorization. The provider verifies a short-lived credential using the existing server-only credential pepper, requires the exact operation and deployment identity, and permits only declared package/media artifacts. TLS is mandatory. The receiver stores chunks in noncanonical temporary storage, verifies each SHA-256, records idempotent PostgreSQL receipts, and assembles only after every declared range exists. It verifies package, Founder, media, control, source/build, target, and authorization digests before acknowledging `provider-prepared`.

The interface never accepts arbitrary destination paths, shell commands, bucket keys, or caller-selected database objects. Raw Windows paths and provider object keys never appear in client responses. Interrupted upload resumes by asking for missing declared chunks. A mismatched replay, extra artifact, expired credential, cross-owner request, or tuple drift fails closed. Temporary artifacts are removed after completion/abort under the approved retention rules.

Pre-staging may contain only synthetic or explicitly nonauthoritative data. The final authoritative package is created and transferred only after the fence.

## PostgreSQL and application composition

Provider full-runtime mode composes all Founder repositories from PostgreSQL. Reads hydrate a fresh canonical runtime. Mutations execute under a PostgreSQL advisory lock and transaction, compare the expected full-runtime fingerprint, apply the application unit of work, persist all changed canonical collections/context/metadata, record provenance and outbox effects, and advance runtime revision. Compatibility mode is restricted to named isolated test databases and can never claim production authority.

Server components, actions, and `/api/v1` share application handlers. Provider mode has fail-closed build replacements for Founder file runtime and protected Windows migration control. There is no JSON fallback or dual write. The same command contracts—idempotency, expected versions, ownership, provenance, replay/drift rejection, suppression, and outbox—apply independently of whether the caller is web or future iOS.

## Media and domain atomicity

New provider uploads use a durable intent and private multipart upload. The server validates declared type/length/hash, performs private readback validation, then commits ownership, canonical media metadata, related record mutation, first-write claim when applicable, and outbox entry in PostgreSQL. A failed upload is aborted; a failed final transaction compensates by deleting the exact object version. Retry is keyed to the intent and identical payload; payload drift is rejected.

Existing migrated objects are accepted only when the final inventory validates count, bytes, MIME, SHA-256, owner, logical media ID, object version, and no public ACL. Clients see `media://` logical references and short opaque application handles. The server redeems them, rechecks owner/expiry, and proxies a signed private read without revealing object keys.

Canonical private-media object IDs are opaque values in either the current migrated form `media-<32 lowercase hex>-<12 lowercase hex>` or the retained UUID form used by provider uploads and legacy records. A provider-backed boot acceptance exposed an obsolete UUID-only check in the private-evidence route; the route and directly related media-reference consumers now use the shared identifier contract. This additive alignment does not accept storage keys or paths: traversal encodings, separators, URLs, control characters, extra route segments, and malformed or overlong values remain rejected before owner-scoped lookup, while ownership and opaque-handle redemption remain mandatory authorization steps.

## Worker handoff

The Windows/background producer is drained before final capture. The provider worker may run in compatibility mode but cannot poll the production outbox until the exact authority tuple is provider-authoritative. Authority-gated polling, durable leases, idempotent handlers, retry/dead-letter rules, and dedupe prevent duplicate canonical effects. The first provider worker heartbeat and outbox health are readiness requirements. Failure after handoff pauses work or enters `recovery-required`; it never restarts a stale Windows consumer.

## Routing handoff

The current Windows public route and any LAN/tunnel fallback remain unchanged in this patch. Before the future window, the exact provider hostname, TLS, rollback mechanics, TTL/dashboard action, and operator are recorded. The route is prepared but not enabled until provider preparation is durable. Windows stays fenced throughout the switch. A pre-first-write routing failure may return traffic to the unchanged fenced/then-unfenced Windows snapshot after the abort verifier passes. After the first provider write, traffic may move only among schema-compatible provider builds; it never returns to a write-enabled stale Windows runtime.

## Rollback and recovery matrix

| Point | Permitted response |
| --- | --- |
| Before fence | Abort; Windows remains authoritative and writable. |
| Fence through provider-prepared, before authority transfer | Remove noncanonical provider artifacts as allowed, verify the Windows snapshot/control are unchanged, release the exact fence, and resume Windows. |
| Authority transferred but no provider canonical write | If exact invariant checks prove no provider write, reverse routing/control once, restore Windows legacy authority, then release its fence. Any uncertainty fails closed. |
| First provider canonical write recorded | PostgreSQL/Spaces and App Platform remain canonical. Pause writes, promote a schema-compatible provider rollback build/spec, and repair/reconcile forward. Windows remains write-disabled. |
| Media completion partially fails | Abort/delete exact noncanonical version or reconcile the durable intent/outbox; never leave an accepted record permanently pointing to missing media. |

Synthetic rehearsal injects failures at fence, snapshot, transfer, import, media, database validation, Spaces validation, read parity, command readiness, routing preparation, acknowledgement, first provider write, media finalization, and worker startup. Pre-boundary cases must restore an unchanged legacy fingerprint; post-boundary cases must enter `recovery-required` with provider authority preserved.

## Readiness and product parity

Compatibility acceptance must cover Home, Log, weight, nutrition, activity, Evidence Review, goals, Operating Plan, priorities, progress, Confidence V2, briefings, training history/day/library/detail, profile, and private media. It compares IDs, values, ordering, dates/timezones, destinations, versions, freshness, and presentation inputs.

Command acceptance covers every Phase 3 command plus logger start/past-workout, area/search/exercise creation, previous performance, set prepopulation/edit/add/remove, variants/supersets, draft/resume/leave/cancel/finish/review, Apple Health reconciliation, Evidence Review handoff, TrainingSession/day/library effects, suggestions, and false/superseded exclusion. Evidence acceptance covers nutrition, training, Apple Health, photo, DEXA, generic intake, review, intent/completion, duplicate/replay, relationships, and private reads. Goals, Operating Plan, protocols, priorities, progress, and published Confidence/briefing ownership must retain generic multi-user contracts with no Founder shortcut.

Provider readiness reports source/build, PostgreSQL connection and schema, Spaces reachability/private policy, worker heartbeat, outbox health, authority/control state, epoch/composition, media readiness, and whether any legacy dependency is enabled. Compatibility mode reports prepared/non-authoritative truthfully and cannot report canonical provider authority.

## Build, security, topology, and cost

`Dockerfile.product` builds the full Next.js standalone runtime separately from `Dockerfile.foundation`. `Dockerfile.provider-worker` builds the worker. Provider build tracing replaces legacy Founder/file control imports with fail-closed modules. The image excludes `private`, `.env*`, logs, recovery material, Founder seed/runtime, personal fixtures, stories, and production evidence; secrets are runtime-injected encrypted variables.

`infra/digitalocean/app.product.template.yaml` retains one 512 MiB web and one 512 MiB worker in the existing app and uses the existing Managed PostgreSQL and private/versioned Space. The database firewall remains App-Platform-only. No new recurring resource is required; the accepted base remains $30.15/month, beneath the $50 ceiling. Capacity must be measured during compatibility deployment before cutover.

## Timing and hard boundaries

The old 2–5 minute persistence-only estimate is invalid for this combined transition. Local synthetic timings are useful only as component measurements. The production write-pause estimate is established after an exact-source provider compatibility deployment and a provider-backed copy rehearsal measuring fence/drain, final package generation, resumable transfer, PostgreSQL import, media copy, semantic parity, startup, routing, and smoke separately.

The future authorization must include the measured range and a hard pre-first-write abort time. Until provider-backed measurement exists, no production window or inherited ten-minute boundary is approved.

## Recovery packet and retention

The final packet is refreshed only after the combined source and compatibility deployment are accepted. It contains the final fenced Windows JSON/media/control state, combined source, exact provider source/build/spec, current and rollback Windows builds, compatible provider rollback build/spec, package-v2 and transfer tooling, authority state/receipts, PostgreSQL and Spaces inventories, routing configuration, and runbooks. It is encrypted, replicated off-machine, restore-tested, and retained for at least 35 days and until all approved exit conditions pass. This document does not refresh or delete the current packet.

## Authorization gates

The mandatory sequence is:

1. Preserve this exact source through End Work Session.
2. Build the exact checkpoint in isolation.
3. Separately authorize and deploy the full provider runtime in non-authoritative compatibility mode.
4. Complete provider-backed full-product acceptance and capacity/timing measurement.
5. Refresh and restore-test the recovery packet for the accepted lineage.
6. Run a fresh combined-cutover preflight.
7. Obtain a new exact GO binding runtime and persistence authority, final Founder/media/control identities, Windows and provider identities, targets, recovery packet, routing, pause/abort/recovery rules, inactive authentication, retention, and the user's no-writes acknowledgement.
8. Execute the production combined cutover.

The prior migration GO is permanently invalid. No compatibility deployment, production fence, production transfer/import, route switch, authentication activation, or iOS implementation is authorized by this runbook.

## Compatibility remediation checkpoint (2026-08-14)

Predeployment artifact inspection found that repository-root `tmp` was distinct from ignored `.tmp`: Next standalone tracing copied a tracked Founder-derived Playwright runtime and a private rendered briefing into the product artifact. The current branch removes those four private/generated `tmp` files in a normal forward change, ignores root `tmp`, excludes it from the Docker context and output tracing, prunes forbidden roots in both runtime images, and runs an independent final-artifact scanner. Historical commits are unchanged; any history-rewrite decision is separate and is not authorized here.

Compatibility authority is now an explicit durable `provider-compatibility-nonauthoritative` state in migration `000005`. It preserves Windows public, migration-control, and legacy canonical authority; disables production writes and combined execution; has no production operation or first-write boundary; and permits only the guarded Phase 5 test/restore databases. Web, worker, repository commands, and media completion revalidate that exact tuple. Production-authoritative behavior continues to require the original strict handoff tuple.

Narrow App Platform read authorization independently captured app `bf57cf56-48cc-4cd6-90e4-a23ee5381741`: rollback deployment `0d27de79-169a-4fda-a16c-ad868d46b7e4` is active at source `73c612a539ba056e5dd3b0634a80859f83910787`, with one 512 MiB `web`, one 512 MiB `worker`, web-only ingress, no custom domain, the live health path, and all eight accepted app/component alerts. The exact rollback spec digest is `2ADB019D77AB20F6AD58AFA5DB7174D539EF2D41A8ED0A04CFB6A5E22BD62602`; encrypted values were not printed or documented. The product template now explicitly preserves those alerts so a full-spec update cannot silently delete them.

Provider-side read-only inspection of `physiqueos_phase5_test_provider_20260811` found schema through `000004`, no authority/transfer row, and only the accepted synthetic fixture: 42 stored collections, 370 records, 764 relationships, and three versioned media objects totaling 111 bytes. The isolated database was then advanced through `000005`, initialized with the explicit non-authoritative compatibility tuple, and rebound by an audited state-version increment to the repaired source after the pre-boot metadata schema contract was corrected. The current 39-collection synthetic package validates at 358 current-shape records and 739 current relationships; 12 superseded synthetic context records remain noncanonical, preserving the original 370-record inventory without destructive cleanup. There is one synthetic owner, no transfer receipt, no production operation, and no first-provider-write marker. These bounded compatibility preparations do not deploy the product or transfer production authority.

Full-provider Home rendering must enter through the selected application composition and its `home` read model. Direct use of the legacy Home service singleton is prohibited in provider full-runtime mode because its legacy runtime-store dependency correctly fails closed. Compatibility acceptance boots the exact standalone artifact against the isolated PostgreSQL/Spaces tuple and requires liveness, compatibility readiness, Home rendering, and a static asset response before deployment authorization.

Artifact acceptance also scans for Founder owner identifiers. Legacy seed-backed singleton repositories are empty unless a composition injects data, so provider bundling cannot pull seed records through unused default exports. The web trace excludes non-runtime scripts/tests, and the worker image is assembled from a deterministic reachable-module graph plus its installed production dependency closure; provider-safe runtime/control modules are substituted and package documentation/examples are omitted before scanning.

## Provider build and Windows lifecycle isolation

The prior compatibility preflight incident was caused by launching a provider full-runtime build from the canonical Windows repository. Because that invocation inherited the default `.next` destination, it overwrote the live Windows artifact before provider acceptance began. Provider full-runtime configuration now fails before Next compilation with `PROVIDER_BUILD_CANONICAL_ROOT_FORBIDDEN` when the working tree resolves to the canonical production root, including a filesystem alias.

Every local provider preflight must use `scripts/runIsolatedProviderBuild.mjs` from a clean, detached checkout of the exact commit. The caller must bind the canonical root, isolated root, exact 40-character source commit, provider build identity, isolated `distDir`, and disjoint artifact directory. The destination must remain inside the isolated checkout and may not resolve through a symbolic link, junction, or reparse path into canonical `.next` or any discovered `.next.rollback-*`, `.next.release-*`, `.next.failed-*`, `.next.fallback-*`, staging, or recovery directory. There is no operator override.

The wrapper snapshots the canonical build/source stamps, file inventory and bounded digest, every recovery-directory inventory, and canonical task PID/start identity before work. It repeats those checks after artifact collection and privacy scanning and blocks with `PROVIDER_WINDOWS_IDENTITY_CHANGED` on any difference; it never repairs, promotes, stops, starts, or rolls back Windows. Provider deployment/rollback tooling is restricted to App Platform state. Windows stop/start, Scheduler ownership, `.next` promotion, fallback restoration, and recovery cleanup remain exclusively in the Windows deployment lifecycle.

## Implementation Phase 1 — cutover authority initialization (2026-08-18)

The Founder approved this document as the target production cutover architecture. Phase 1 begins its production wiring and is limited to durable authority-record preparation.

`physiqueos.combined_runtime_authority.environment` is the table's primary key and the authority store is constructed per environment, so the production cutover record is a **separate row under its own environment key** rather than a mutation of the isolated compatibility record. No compatibility-to-Windows-legacy transition and no schema change are required, and the compatibility tuple is neither moved nor rewritten. `createCompatibilityRuntimeAuthorityState` additionally refuses any environment outside `/^compatibility(?:[-/]|$)/i`, so a compatibility tuple cannot exist under a production key even by accident.

`initializeCombinedCutoverAuthority` (`src/platform/cutover/CombinedCutoverAuthorityInitializer.js`) is the single production entry point. It seeds only `windows-legacy-authoritative`, refuses compatibility-shaped environments, requires the combined schema, refuses a database already carrying a combined transfer receipt, relies on the store's idempotent initialization (divergent existing rows fail closed rather than being overwritten), and re-reads and re-asserts the persisted tuple. It cannot set provider authority, an operation, a fence, or a first-write boundary; authority transfer remains exclusively `TRANSFER_TO_PROVIDER` under `CombinedAppPlatformCutoverOrchestrator`. `assertCombinedCutoverPreCutoverAuthorityState` is the reusable pre-cutover readiness predicate.

The synthetic combined-cutover rehearsal remains unimplemented. The governing requirement is that it exercise `claimCanonicalWriteBoundary` through real transaction semantics rather than a test-only flag; the repository currently has no local PostgreSQL test fixture (no `pg-mem`, no test containers, and every PostgreSQL-touching test uses a hand-written stub), so a transaction-faithful fixture — including true rollback of uncommitted mutations, which is the whole substance of the atomicity proof — is a prerequisite and is deferred to Phase 2.

## Implementation Phase 2 — transactional fixture and synthetic rehearsal (2026-08-18)

Phase 2A added `src/platform/database/testing/transactionalPostgresFixture.js`, a transaction-faithful in-memory substrate modelling BEGIN/COMMIT/ROLLBACK with real staging, `SELECT ... FOR UPDATE`, transaction-scoped `pg_advisory_xact_lock`, the optimistic version guard, append-only audit inserts, canonical record upserts, and deterministic error injection. Unmodeled SQL throws rather than returning an empty result. Its limitations are documented in the module header: snapshot-at-BEGIN plus explicit locking, not MVCC, and lock conflicts fail deterministically rather than blocking.

Phase 2B added `src/platform/cutover/syntheticCombinedCutoverRehearsal.js` and `scripts/runSyntheticCombinedCutoverRehearsal.mjs`.

**Real production logic exercised:** `createCombinedAppPlatformCutoverOrchestrator`, `CombinedRuntimeAuthorityState` (BEGIN_CUTOVER, ACKNOWLEDGE_PROVIDER, TRANSFER_TO_PROVIDER, RECORD_FIRST_PROVIDER_WRITE, ABORT_TO_WINDOWS, REQUIRE_RECOVERY), `PostgresCombinedRuntimeAuthorityStore` including `claimCanonicalWriteBoundary`, `CombinedCutoverAuthorityInitializer`, and `createPhase4CanonicalRecordStore`. `firstProviderCanonicalWriteAt` is never set directly by the harness; it is established only by the real authority-protected transaction.

**Synthetic adapters:** all six preflights plus `activateWindowsWriteFence`, `captureFinalSnapshot`, `exportFinalPackage`, `transferSnapshot` (digest-bound, idempotent receipts), `importProviderCanonicalState`, `verifyProviderParity`, `acknowledgeProviderPrepared`, `transferAuthorityAndRoute` (routing and worker handoff simulated in-memory), `verifyPostHandoff`, `restoreWindowsAuthority`, and `enterProviderRecovery`.

**Boundary placement.** The orchestrator has no first-provider-write stage, which matches phase M: the boundary is crossed by the first real canonical command through the provider application, which is why `claimCanonicalWriteBoundary` lives in the repository facade. The rehearsal drives the orchestrator through phase L and then crosses the boundary separately through the real write path.

**Recovery ownership.** `inspectCombinedCutoverRecovery` makes provider-side durable evidence authoritative: when `firstProviderCanonicalWriteAt` is set, no local state — including a missing or stale migration-control `firstPostgresWriteAt` — may readmit a Windows rollback or a pre-boundary restart. Local `firstPostgresWriteAt` is not consulted by the combined rehearsal at all.

**Still unimplemented for production:** the authenticated provider transfer endpoint, real public routing handoff, real provider worker handoff, real provider deployment of the product image, and combined-cutover recovery evidence capture. Production cutover remains blocked until those exist and a full rehearsal passes against them.

## Implementation Phase 3 — authenticated provider transfer service (2026-08-18)

Phase 3 implements the production authenticated transfer channel behind the synthetic `transferSnapshot` contract that Phase 2B proved (`src/platform/cutover/syntheticCombinedCutoverRehearsal.js`), and the real Windows-side adapter that drives it. It is limited to transfer plumbing: no public routing handoff, no real worker handoff, no provider deployment, and no complete production adapter set. Transfer alone never imports canonical data, transfers runtime authority, sets `firstProviderCanonicalWriteAt`, enables provider production writes, or switches routing — every module in `src/platform/cutover/transfer/` is statically proven (`CombinedCutoverTransferAuthorityIsolation.test.js`) to import nothing from the runtime-authority state machine, the authority store, the canonical record store, or the worker.

**Two layers, two receipt tables, deliberately not merged.** The existing `physiqueos.combined_transfer_receipts` (migration 000005, previously unused by any HTTP surface) is reused unchanged as the OPERATION-level manifest declaration and final cross-checked completion: one row per `migrationOperationId`, bound to `packageDigest`/`runtimeSha256`/`mediaInventorySha256`/`migrationControlSha256`, idempotent on an identical redeclare, rejecting drift. A new, additive migration `000006_combined_cutover_transfer_staging.cjs` adds `physiqueos.combined_cutover_transfer_receipts` and `physiqueos.combined_cutover_transfer_chunks` for the BYTE-level, chunked, resumable transfer of each individual package artifact (`manifest.json`, `canonical-runtime.json`, and every media file — each identified by an opaque `packageId` derived from its package-relative path, never by a raw path). The two tables model different facts with different lifetimes — "this operation is authorized to transfer this package" versus "3 of 41 chunks of media file 7 have landed" — so they are not overloaded into one schema.

**Chunking and size.** A realistic production package (Founder runtime revision 140, 30,262,748 bytes, plus 402 media files / 288,919,315 bytes) is far larger than is safe to assume as one HTTP request through the App Platform proxy into a 512 MiB instance, so transfer is chunked and resumable by construction (default 8 MiB, capped at 16 MiB per chunk). Every chunk is bound to `(operationId, packageId, overallDigest, chunkIndex, chunkDigest, expectedChunkCount, expectedBytes)`. The server independently recomputes each chunk's SHA-256 from the bytes it actually received and rejects any mismatch; a chunk's declared Content-Length is checked before the body is buffered.

**Authentication is separate from the Founder access gate.** `combinedCutoverTransferAuth.js` verifies a short-lived, operation-bound machine credential using the existing server-only credential pepper and `hashHighEntropyCredential`/`verifyHighEntropyCredential` (`src/platform/auth/credentialHash.js`) — the same primitive the Founder gate's session tokens do not use but that this transfer channel does, reusing an existing pattern rather than inventing a new one. Only the credential's HMAC is ever held by the provider; the plaintext exists only in the Windows client's environment. The credential is scoped to exactly one `migrationOperationId`, carries an explicit `PHYSIQUEOS_COMBINED_CUTOVER_TRANSFER_CREDENTIAL_EXPIRES_AT`, is revocable by clearing or rotating one environment value, and authorizes byte transfer only. Missing or malformed configuration disables the whole channel (`TRANSFER_NOT_CONFIGURED`) rather than degrading to unauthenticated. Because the transfer routes (`/api/v1/operations/combined-cutover/transfer/**`) are machine-to-machine, they are exempted from the Founder session-cookie gate in `src/platform/accessGate/publicRoutes.js` — deliberately, not by omission — while remaining fully protected by this separate, fail-closed machine authentication underneath. Authentication never substitutes for operation/package/digest validation: every authenticated request is still fully validated by the receipt stores.

**Staging substrate.** Received chunk bytes are non-authoritative and are staged in the existing private, versioned Space rather than the App Platform container filesystem, which the compatibility-remediation history in this document already established is ephemeral and unsuitable for durable evidence. Staged objects live under a dedicated `cutover-transfer/<operationId>/<packageId>/chunks/<ordinal>` namespace, wholly disjoint from canonical Founder media's `private/<ownerUserId>/<objectId>/` namespace (`SpacesPrivateObjectProvider.js`'s `createPrivateObjectKey`). Keys are constructed server-side only from already-validated opaque identifiers and are re-checked against a whole-string pattern before every object operation, so no traversal, separator, or namespace escape is possible and the Windows client is never handed a bucket key, a signed URL, or any canonical-media write capability.

**Independent verification, never client-asserted.** A byte-level artifact is marked `verified` only after the provider assembles every received chunk from staging in order and independently recomputes the whole-artifact SHA-256 — a mismatch marks the receipt `failed`, never `verified`. The operation-level manifest is marked `verified` only after the provider mechanically cross-checks every declared file's own already-verified byte-level receipt against the manifest's declared digest and byte length; the client never submits a verification result, only an operation identity.

**Idempotency and resume.** Declaring the same `(operationId, packageId)` twice with identical geometry is a no-op idempotent replay; a differing digest, size, or chunk count fails closed (`TRANSFER_PACKAGE_DIGEST_CONFLICT` / `TRANSFER_PACKAGE_SIZE_CONFLICT` / `TRANSFER_CHUNK_COUNT_CONFLICT`). Re-uploading an identical chunk is always a safe idempotent replay verified by digest, not merely presence; a conflicting chunk at the same index fails closed (`TRANSFER_CHUNK_CONFLICT`). One operation's credential can never act on another operation's transfer (`TRANSFER_OPERATION_FORBIDDEN`), and status reads are scoped strictly to the caller's own `(operationId, packageId)`. The Windows adapter's resume strategy is deliberately simple rather than bandwidth-optimal: on resume it redeclares (idempotent) and re-sends every chunk, relying on cheap idempotent replay for already-received chunks rather than tracking exactly which indices landed — correct and resumable, with bandwidth-efficient resume left as later work.

**Durable receipts.** The byte-level receipt (`combined_cutover_transfer_receipts`) carries `schemaVersion`, `receiptId`, `operationId`, `packageId`, `overallDigest`, `expectedBytes`, `receivedBytes`, `expectedChunkCount`, `receivedChunkCount`, `status`, `createdAt`, `updatedAt`, `completedAt`, `verifiedAt` — never payload bytes. Neither receipt table nor any response projection (`publicReceipt`/`publicManifestReceipt`) ever echoes staged bytes, staging keys, Windows paths, Spaces keys, or credentials.

**Windows-side adapter.** `WindowsCombinedCutoverTransferClient.js` implements `createCombinedCutoverTransferHttpClient` (HTTPS-only, bearer machine credential, bounded retry) and `createProductionTransferSnapshotAdapter`, which preserves the synthetic rehearsal's `async ({ input, state, snapshot, exported }) => receipt` interface exactly, so it can be substituted into `CombinedAppPlatformCutoverOrchestrator` without changing the orchestrator. It refuses to transfer if the exported package's digest does not match the fenced snapshot already bound into runtime authority by `BEGIN_CUTOVER`. Automatic retry is bounded and applies only to network-level failures and the small set of explicitly retryable codes (`TRANSFER_STAGING_UNAVAILABLE`, `TRANSFER_TRANSPORT_FAILED`); every semantic rejection — authentication, operation binding, digest/size/count conflict, chunk conflict — is raised immediately, never retried.

**What remains blocked after Phase 3.** Real public routing handoff, real provider worker handoff, real provider deployment of the product image, `importProviderCanonicalState` (real canonical PostgreSQL/Spaces import), real `verifyProviderParity`/`acknowledgeProviderPrepared`/`transferAuthorityAndRoute`/`verifyPostHandoff` adapters, and combined-cutover recovery evidence capture. Production cutover remains blocked until those exist and a full rehearsal passes against them under this transfer service. The prior migration GO remains permanently invalid; nothing in Phase 3 authorizes deployment, a Trusted Source, a DigitalOcean PAT, a production fence, provider authority transfer, a routing switch, stopping Windows, or Founder GO.

## Implementation Phase 4 — canonical import, provider parity, and provider-prepared acknowledgement (2026-08-18)

Phase 4 converts three more synthetic orchestrator adapters to production-capable implementations: `importProviderCanonicalState`, `verifyProviderParity`, and `acknowledgeProviderPrepared`. It stops at that boundary: `transferAuthorityAndRoute`, real public routing, and real worker handoff remain synthetic-only, and nothing here deploys, transfers authority, or mutates live `combined_runtime_authority`/migration-control.

**Architecture mirrors Phase 3 exactly, for the same reason.** The database firewall is App-Platform-only, so - just like `transferSnapshot` - these three adapters must cross the machine boundary. Production logic lives in provider-side `src/platform/cutover/preparation/` service modules; a new authenticated internal route family (`/api/v1/operations/combined-cutover/prepare/{import,parity,acknowledge,status}`), exempted from the Founder session gate the same way Phase 3's transfer routes are, exposes them; and `WindowsCombinedCutoverPreparationClient.js` implements the three Windows-side orchestrator adapters as HTTP clients against that route family, preserving the synthetic rehearsal's exact interfaces. A dedicated machine credential (`combinedCutoverPreparationAuth.js`, `PHYSIQUEOS_COMBINED_CUTOVER_PREPARE_*`) - separate from Phase 3's transfer credential on purpose, since import genuinely writes canonical data while transfer only moves bytes - gates the whole family with the same fail-closed, operation-bound, `hashHighEntropyCredential`-based pattern Phase 3 established.

**Import reuses the existing canonical mapping unchanged.** `ProductionCanonicalImportService.js` never trusts client-asserted completion: it independently re-reads the operation-level transfer receipt (`combined_transfer_receipts`, migration 000005) and requires `status='verified'` with matching authorization/fence/package identity, then re-verifies every declared artifact's own byte-level receipt (migration 000006) via `verifyManifestArtifactsAgainstReceipts` - the exact cross-check Phase 3's manifest completion uses, extracted into `combinedCutoverManifestArtifactVerification.js` so the two can never independently drift. Only then does it assemble `manifest.json`/`canonical-runtime.json` bytes from Spaces transfer staging (`artifactReceiptStore.readVerifiedBytes`, which re-digests before returning), materialize them into a short-lived `os.tmpdir()` package directory (`combinedCutoverArtifactAssembly.js` - transient working memory, not durable staging; always cleaned up), and call the unmodified `importCanonicalPackage`/`migrateCanonicalPackageMediaToSpaces` from the older single-machine migration path. `ProductionSpacesMediaMigration.js` gained one additive `readSourceBytes` injection point so media bytes can come from Spaces transfer staging instead of a local snapshot directory, with zero duplication of the upload/readback-verify/DB-update sequence. Import writes directly through that machinery's own SQL, which never touches `combined_runtime_authority` or `claimCanonicalWriteBoundary` - exactly like the pre-authority-transfer old migration path - so it remains a staging operation that confers no authority, sets no `firstProviderCanonicalWriteAt`, and enables no production writes. A failed or partial import is left in place rather than destructively rolled back: because authority never transferred, those rows are unreachable through any authorized read path, and every write is an upsert, so a retried import safely converges.

**Parity reuses the corrected live read-parity architecture unchanged.** The volatile-envelope-field exclusion (exactly `generatedAt`/`freshThrough`/`etag`), the one-shared-frozen-clock requirement, and the bounded non-dumping diagnostics were extracted from `scripts/productionMigrationEnvironmentAdapters.mjs` into `src/platform/migration/readModelParityComparison.js` (the script now re-exports them for backward compatibility, so its own extensive regression suite continues to exercise this exact implementation) and are reused verbatim by `ProductionProviderParityService.js` - no field was added to the exclusion set, nothing was weakened. The source side of parity is the final fenced Windows package: the same verified transfer artifacts import already consumed, re-assembled and compared with a package-derived `{version, revision, updatedAt}` merge exactly like the old path's `verifyReadParity`, never live mutable Windows state. The provider side is the just-imported PostgreSQL canonical state, read fresh through `createPhase5ProviderApplicationComposition` under the same frozen clock. Canonical collection identity/counts and semantic-digest parity reuse `validateCanonicalImport` unchanged. Media/object inventory parity is new: exact count match against `canonical_media_objects` (no missing, no unexpected owner-scoped object), byte/digest agreement with the declared package, and independent digest/byte-length re-verification via `objectProvider.inspectObject` - which returns only byte length and hash, never a signed URL or object content. Minimal command readiness (whether the provider composition exposes a working command boundary) is folded into parity's own result because `CombinedAppPlatformCutoverOrchestrator` has no separate command-readiness stage; it is a presence check only and never executes a write, so it does not duplicate acknowledgement's job.

**Acknowledgement never mutates authority; it only reads and gates.** `ProductionAcknowledgeProviderPreparedService.js` calls `authorityStore.read()` to independently verify `authority === combined-cutover-in-progress`, exact operation/authorization-fingerprint/fence/package-digest match, `firstProviderCanonicalWriteAt` still null, `publicRuntimeAuthority === 'windows'`, and `writesEnabled === false`, then re-verifies the transfer and requires durable preparation evidence showing import/media succeeded and parity passed - never trusting in-memory continuity from a single orchestrator run, so a provider process restart between phases changes nothing about what it can determine. Only then does it return the acknowledgement object; `CombinedAppPlatformCutoverOrchestrator` remains the only caller that ever invokes `authorityStore.transition(ACKNOWLEDGE_PROVIDER, ...)`, and `CombinedRuntimeAuthorityState`'s own transition validator independently re-checks the same tuple before committing. This module cannot execute `TRANSFER_TO_PROVIDER`, set `firstProviderCanonicalWriteAt`, change routing, or start a worker - none of those are reachable from anything it does, which `CombinedCutoverPreparationAuthorityIsolation.test.js` proves statically (it is the only Phase 4 module permitted to import the runtime-authority state module at all, and only to read it).

**Durable evidence: a new, narrow, additive table.** `phase4_import_runs` (migration 000003) was found insufficient for this role: its primary key is a `migrationId` derived only from the source runtime's SHA-256, with no `operationId` dimension at all, and its `ON CONFLICT DO UPDATE` would silently accept a conflicting `package_digest` for an unchanged `migrationId` - exactly the "silently overwrite another operation's evidence" failure this phase must refuse. It remains untouched for the old path. Migration `000007_combined_cutover_preparation_evidence.cjs` adds one compact, operation-bound `combined_cutover_preparation_receipts` row per cutover operation (`UNIQUE migration_operation_id`) covering import/media/parity/prepared status, timestamps, collection counts, and digests - never payload contents - with a database `CHECK` enforcing that a row can only reach `prepared_status='acknowledged'` once import, media, and parity have all independently succeeded. Every write method requires and re-checks an `expectedPackageDigest` against the durable row, so a stale or conflicting caller fails closed rather than corrupting evidence.

**Idempotency.** An import already `succeeded` for the exact operation/package returns the recorded result without re-running; a conflicting package digest for the same operation fails closed at `preparationStore.declare`. Parity already `passed` returns immediately. Acknowledgement already `acknowledged` returns the same deterministic acknowledgement object. None of the three durable-evidence writes ever silently overwrites another operation's row.

**What remains blocked after Phase 4.** `transferAuthorityAndRoute` (real authority/routing handoff), real public routing, real provider worker handoff, real provider deployment of the product image, and combined-cutover recovery evidence capture. Production cutover remains blocked until those exist and a full rehearsal passes against them. Nothing in Phase 4 authorizes deployment, a Trusted Source, a DigitalOcean PAT, a production fence, provider authority transfer, a routing switch, stopping Windows, or Founder GO.
