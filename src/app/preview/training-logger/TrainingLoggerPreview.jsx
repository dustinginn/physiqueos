"use client";

import TrainingLoggerClient from "../../../components/training/TrainingLoggerClient";

export default function TrainingLoggerPreview({ initialDate }) {
  return <TrainingLoggerClient initialDate={initialDate} />;
}
