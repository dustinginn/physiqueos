import PhotoSimulatorScreen from "../../screens/PhotoSimulatorScreen";

export const metadata = {
  title: "Photo Upload Simulator | PhysiqueOS",
};

export default function PhotoSimulatorPage() {
  return <PhotoSimulatorScreen />;
}
