import {FounderRepositories} from "../../../data/repositories/founderRepositories";
import {createWeeklyNarrativeService} from "../../../domain/services/WeeklyNarrativeService";
import WeeklyBriefingScreen from "../../../screens/WeeklyBriefingScreen";
import PersistedLegacyWeeklyScreen from "../../../screens/DailyBriefingScreen";
export const dynamic="force-dynamic";
export default async function WeeklyPage(){const user=await FounderRepositories.users.getCurrentUser();const artifact=await createWeeklyNarrativeService({repositories:FounderRepositories}).getLatest({userId:user.id});if(!artifact)return <main className="app-surface min-h-screen"><div className="mx-auto max-w-[393px] px-4 py-10"><p className="text-sm font-extrabold uppercase tracking-[0.12em] text-[var(--primary)]">Weekly Briefing</p><h1 className="mt-3 text-3xl font-extrabold text-[var(--text-primary)]">No persisted Weekly Briefing is available yet.</h1></div></main>;if(!artifact.briefing?.weeklyNarrative)return <PersistedLegacyWeeklyScreen backHref="/briefings/review" backLabel="Briefing History" briefing={artifact.briefing} eyebrow="Weekly Briefing"/>;return <WeeklyBriefingScreen narrative={artifact.briefing.weeklyNarrative}/>;}
